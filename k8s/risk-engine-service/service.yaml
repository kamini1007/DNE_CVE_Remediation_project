apiVersion: v1
kind: Service
metadata:
  name: risk-engine-service
  namespace: cve-platform
spec:
  selector:
    app: risk-engine-service
  ports:
    - name: http
      port: 80
      targetPort: 8083
  type: ClusterIP
