apiVersion: v1
kind: Service
metadata:
  name: learning-service
  namespace: cve-platform
spec:
  selector:
    app: learning-service
  ports:
    - name: http
      port: 80
      targetPort: 3002
  type: ClusterIP
