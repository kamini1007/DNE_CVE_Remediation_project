apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: cve-platform
spec:
  selector:
    app: ingestion-service
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
