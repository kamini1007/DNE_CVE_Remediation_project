apiVersion: v1
kind: Service
metadata:
  name: ai-analysis-service
  namespace: cve-platform
spec:
  selector:
    app: ai-analysis-service
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
