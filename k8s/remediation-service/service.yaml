apiVersion: v1
kind: Service
metadata:
  name: remediation-service
  namespace: cve-platform
spec:
  selector:
    app: remediation-service
  ports:
    - name: http
      port: 80
      targetPort: 3001
  type: ClusterIP
